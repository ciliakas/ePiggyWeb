@page
@using ePiggyWeb.DataManagement
@using ePiggyWeb.Utilities
@model ePiggyWeb.Pages.EntryModel
<!DOCTYPE html>
<html>
<body>
    @{
        var entry = new DataEntry();
        var entries = new DataEntries(EntryType.Income)
        {
        new DataEntry(),
        new DataEntry(),
        new DataEntry()
        };
        entries.Add(entry);
    }
    <h1>Hello Web Pages</h1>
    <p>@entries</p>
</body>
</html> 