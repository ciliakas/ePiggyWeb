namespace ePiggyWeb.Utilities
{
    public enum EntryType
    {
        Income,
        Expense
    }
    public enum Importance
    {
        Necessary = 1,
        High,
        Medium,
        Low,
        Unnecessary
    }
    public enum SavingType
    {
        Maximal,
        Regular,
        Minimal
    }
}

